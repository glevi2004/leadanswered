# --- Redis (required to run the worker) ---
REDIS_URL=redis://localhost:6379

# --- Supabase Postgres (same DB as the api) ---
DB_PASSWORD=your-db-password
DATABASE_URL=postgresql://postgres.<project-ref>:${DB_PASSWORD}@<pooler-host>:5432/postgres

# --- Twilio (to send the nudge from the contractor's number) ---
TWILIO_ACCOUNT_SID=ACxxxxxxxx
TWILIO_AUTH_TOKEN=xxxxxxxx

# --- Observability (optional) ---
# LANGFUSE_PUBLIC_KEY=pk-lf-...
# LANGFUSE_SECRET_KEY=sk-lf-...
