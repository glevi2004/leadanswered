// Entry point for the department's Database console — the frame's DatabaseConsoleSlot
// lazy-imports `./console/index`, so re-export the console component as the default.
export { default } from "./DatabaseConsole";
