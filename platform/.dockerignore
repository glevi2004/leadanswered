# Keep the image lean + never bake secrets or build artifacts into it.
**/node_modules
**/.next
**/dist
**/.turbo
.git
.agents
**/*.log
# Secrets — never copy local env files into the image.
**/.env
**/.env.*
!**/.env.example
